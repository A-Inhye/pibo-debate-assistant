# WhisperLiveKit 백엔드 의존성
# RTX 3090 + CUDA 환경

# ===== 핵심 의존성 (WhisperLiveKit) =====
fastapi
uvicorn
websockets
torch>=2.0.0
torchaudio>=2.0.0
huggingface-hub>=0.25.0
faster-whisper>=1.2.0
librosa
soundfile
tqdm
tiktoken

# Linux x86_64 전용 (Windows에서는 설치 불필요)
# triton>=2.0.0  # Linux only

# ===== 마이크 캡처 =====
pyaudio
numpy

# ===== VAD (Voice Activity Detection) =====
onnxruntime  # CPU 버전
# onnxruntime-gpu  # GPU 버전 (선택사항)

# ===== 화자 분할 - Sortformer (권장) =====
# NeMo는 크기가 크므로 별도 설치:
# pip install git+https://github.com/NVIDIA/NeMo.git@main#egg=nemo_toolkit[asr]

# ===== 화자 분할 - Diart (대안) =====
# pip install diart pyannote.audio

# ===== 선택적 의존성 =====
# nllw  # 번역 기능: pip install nllw
# mlx-whisper  # Apple Silicon: pip install mlx-whisper
